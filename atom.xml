<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>微雨问海棠</title>
  <subtitle>随笔、笔记、碎碎念</subtitle>
  <link href="https://mizuki.mysqil.com/" rel="alternate" type="text/html"/>
  <link href="https://mizuki.mysqil.com/atom.xml" rel="self" type="application/atom+xml"/>
  <id>https://mizuki.mysqil.com/</id>
  <updated>2025-12-11T13:59:13.592Z</updated>
  <language>zh_CN</language>
  <entry>
    <title>Markdown 教程</title>
    <link href="https://mizuki.mysqil.com/posts/markdown-tutorial/" rel="alternate" type="text/html"/>
    <id>https://mizuki.mysqil.com/posts/markdown-tutorial/</id>
    <published>2025-01-20T00:00:00.000Z</published>
    <updated>2025-01-20T00:00:00.000Z</updated>
    <summary>一个简单的 Markdown 博客文章示例。</summary>
    <content type="html"><![CDATA[<h1>Markdown 教程</h1>
<p>一个 Markdown 示例展示了如何编写 Markdown 文件。本文档整合了核心语法和扩展（GMF）。</p>
<ul>
<li><a href="#block-elements">块级元素</a>
<ul>
<li><a href="#paragraphs-and-line-breaks">段落和换行</a></li>
<li><a href="#headers">标题</a></li>
<li><a href="#blockquotes">引用</a></li>
<li><a href="#lists">列表</a></li>
<li><a href="#code-blocks">代码块</a></li>
<li><a href="#horizontal-rules">水平线</a></li>
<li><a href="#table">表格</a></li>
</ul>
</li>
<li><a href="#span-elements">行内元素</a>
<ul>
<li><a href="#links">链接</a></li>
<li><a href="#emphasis">强调</a></li>
<li><a href="#code">代码</a></li>
<li><a href="#images">图片</a></li>
<li><a href="#strikethrough">删除线</a></li>
</ul>
</li>
<li><a href="#miscellaneous">其他</a>
<ul>
<li><a href="#automatic-links">自动链接</a></li>
<li><a href="#backslash-escapes">反斜杠转义</a></li>
</ul>
</li>
<li><a href="#inline-html">行内 HTML</a></li>
</ul>
<h2>块级元素</h2>
<h3>段落和换行</h3>
<h4>段落</h4>
<p>HTML 标签：<code>&lt;p&gt;</code></p>
<p>一个或多个空行。（空行是指只包含<strong>空格</strong>或<strong>制表符</strong>的行。）</p>
<p>代码：</p>
<pre><code>这将是
行内的。

这是第二段。
</code></pre>
<p>预览：</p>
<hr />
<p>这将是
行内的。</p>
<p>这是第二段。</p>
<hr />
<h4>换行</h4>
<p>HTML 标签：<code>&lt;br /&gt;</code></p>
<p>在行末使用<strong>两个或更多空格</strong>。</p>
<p>代码：</p>
<pre><code>这不会
行内。
</code></pre>
<p>预览：</p>
<hr />
<p>这不会<br />
行内。</p>
<hr />
<h3>标题</h3>
<p>Markdown 支持两种标题样式：Setext 和 atx。</p>
<h4>Setext</h4>
<p>HTML 标签：<code>&lt;h1&gt;</code>、<code>&lt;h2&gt;</code></p>
<p>使用任意数量的<strong>等号 (=)</strong> 作为 <code>&lt;h1&gt;</code> 和<strong>破折号 (-)</strong> 作为 <code>&lt;h2&gt;</code> 来"下划线"。</p>
<p>代码：</p>
<pre><code>这是一级标题
=============
这是二级标题
-------------
</code></pre>
<p>预览：</p>
<hr />
<h1>这是一级标题</h1>
<h2>这是二级标题</h2>
<hr />
<h4>atx</h4>
<p>HTML 标签：<code>&lt;h1&gt;</code>、<code>&lt;h2&gt;</code>、<code>&lt;h3&gt;</code>、<code>&lt;h4&gt;</code>、<code>&lt;h5&gt;</code>、<code>&lt;h6&gt;</code></p>
<p>在行首使用 1-6 个<strong>井号 (#)</strong>，对应 <code>&lt;h1&gt;</code> - <code>&lt;h6&gt;</code>。</p>
<p>代码：</p>
<pre><code># 这是一级标题
## 这是二级标题
###### 这是六级标题
</code></pre>
<p>预览：</p>
<hr />
<h1>这是一级标题</h1>
<h2>这是二级标题</h2>
<h6>这是六级标题</h6>
<hr />
<p>可选地，你可以"闭合" atx 样式的标题。闭合的井号<strong>不需要匹配</strong>用于打开标题的井号数量。</p>
<p>代码：</p>
<pre><code># 这是一级标题 #
## 这是二级标题 ##
### 这是三级标题 ######
</code></pre>
<p>预览：</p>
<hr />
<h1>这是一级标题</h1>
<h2>这是二级标题</h2>
<h3>这是三级标题</h3>
<hr />
<h3>引用</h3>
<p>HTML 标签：<code>&lt;blockquote&gt;</code></p>
<p>Markdown 使用邮件风格的 <strong>&gt;</strong> 字符进行引用。如果你硬换行文本并在每行前放置 &gt;，效果最好。</p>
<p>代码：</p>
<pre><code>&gt; 这是一个包含两段的引用。Lorem ipsum dolor sit amet,
&gt; consectetuer adipiscing elit. Aliquam hendrerit mi posuere lectus.
&gt; Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.
&gt;
&gt; Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse
&gt; id sem consectetuer libero luctus adipiscing.
</code></pre>
<p>预览：</p>
<hr />
<blockquote>
<p>This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,
consectetuer adipiscing elit. Aliquam hendrerit mi posuere lectus.
Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.</p>
<p>Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse
id sem consectetuer libero luctus adipiscing.</p>
</blockquote>
<hr />
<p>Markdown 允许你偷懒，只在硬换行段落的第一行前放置 &gt;。</p>
<p>代码：</p>
<pre><code>&gt; This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,
consectetuer adipiscing elit. Aliquam hendrerit mi posuere lectus.
Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.

&gt; Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse
id sem consectetuer libero luctus adipiscing.
</code></pre>
<p>预览：</p>
<hr />
<blockquote>
<p>This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,
consectetuer adipiscing elit. Aliquam hendrerit mi posuere lectus.
Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.</p>
</blockquote>
<blockquote>
<p>Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse
id sem consectetuer libero luctus adipiscing.</p>
</blockquote>
<hr />
<p>引用可以嵌套（即引用中的引用），通过添加额外的 &gt; 层级。</p>
<p>代码：</p>
<pre><code>&gt; This is the first level of quoting.
&gt;
&gt; &gt; This is nested blockquote.
&gt;
&gt; Back to the first level.
</code></pre>
<p>预览：</p>
<hr />
<blockquote>
<p>This is the first level of quoting.</p>
<blockquote>
<p>This is nested blockquote.</p>
</blockquote>
<p>Back to the first level.</p>
</blockquote>
<hr />
<p>引用可以包含其他 Markdown 元素，包括标题、列表和代码块。</p>
<p>代码：</p>
<pre><code>&gt; ## This is a header.
&gt;
&gt; 1.   This is the first list item.
&gt; 2.   This is the second list item.
&gt;
&gt; Here's some example code:
&gt;
&gt;     return shell_exec("echo $input | $markdown_script");
</code></pre>
<p>预览：</p>
<hr />
<blockquote>
<h2>This is a header.</h2>
<ol>
<li>This is the first list item.</li>
<li>This is the second list item.</li>
</ol>
<p>Here's some example code:</p>
<pre><code>return shell_exec("echo $input | $markdown_script");
</code></pre>
</blockquote>
<hr />
<h3>列表</h3>
<p>Markdown 支持有序（编号）和无序（项目符号）列表。</p>
<h4>无序列表</h4>
<p>HTML 标签：<code>&lt;ul&gt;</code></p>
<p>无序列表使用<strong>星号 (*)</strong>、<strong>加号 (+)</strong> 和<strong>连字符 (-)</strong>。</p>
<p>代码：</p>
<pre><code>*   红色
*   绿色
*   蓝色
</code></pre>
<p>预览：</p>
<hr />
<ul>
<li>Red</li>
<li>Green</li>
<li>Blue</li>
</ul>
<hr />
<p>等价于：</p>
<p>代码：</p>
<pre><code>+   红色
+   绿色
+   蓝色
</code></pre>
<p>和：</p>
<p>代码：</p>
<pre><code>-   红色
-   绿色
-   蓝色
</code></pre>
<h4>有序列表</h4>
<p>HTML 标签：<code>&lt;ol&gt;</code></p>
<p>有序列表使用数字后跟句点：</p>
<p>代码：</p>
<pre><code>1.  Bird
2.  McHale
3.  Parish
</code></pre>
<p>预览：</p>
<hr />
<ol>
<li>Bird</li>
<li>McHale</li>
<li>Parish</li>
</ol>
<hr />
<p>It's possible to trigger an ordered list by accident, by writing something like this:</p>
<p>代码：</p>
<pre><code>1986. What a great season.
</code></pre>
<p>预览：</p>
<hr />
<ol>
<li>What a great season.</li>
</ol>
<hr />
<p>You can <strong>backslash-escape (\)</strong> the period:</p>
<p>代码：</p>
<pre><code>1986\. What a great season.
</code></pre>
<p>预览：</p>
<hr />
<p>1986. What a great season.</p>
<hr />
<h4>缩进列表</h4>
<h5>引用中的列表</h5>
<p>要在列表项中放置引用，引用的 &gt; 分隔符需要缩进：</p>
<p>代码：</p>
<pre><code>*   A list item with a blockquote:

    &gt; This is a blockquote
    &gt; inside a list item.
</code></pre>
<p>预览：</p>
<hr />
<ul>
<li>
<p>A list item with a blockquote:</p>
<blockquote>
<p>This is a blockquote
inside a list item.</p>
</blockquote>
</li>
</ul>
<hr />
<h5>代码块中的列表</h5>
<p>要在列表项中放置代码块，代码块需要缩进两次——<strong>8 个空格</strong>或<strong>两个制表符</strong>：</p>
<p>代码：</p>
<pre><code>*   A list item with a code block:

        &lt;code goes here&gt;
</code></pre>
<p>预览：</p>
<hr />
<ul>
<li>
<p>A list item with a code block:</p>
<pre><code>&lt;code goes here&gt;
</code></pre>
</li>
</ul>
<hr />
<h5>嵌套列表</h5>
<p>代码：</p>
<pre><code>* A
  * A1
  * A2
* B
* C
</code></pre>
<p>预览：</p>
<hr />
<ul>
<li>A
<ul>
<li>A1</li>
<li>A2</li>
</ul>
</li>
<li>B</li>
<li>C</li>
</ul>
<hr />
<h3>代码块</h3>
<p>HTML 标签：<code>&lt;pre&gt;</code></p>
<p>将块的每一行至少缩进<strong>4 个空格</strong>或<strong>1 个制表符</strong>。</p>
<p>代码：</p>
<pre><code>This is a normal paragraph:

    This is a code block.
</code></pre>
<p>预览：</p>
<hr />
<p>This is a normal paragraph:</p>
<pre><code>This is a code block.
</code></pre>
<hr />
<p>A code block continues until it reaches a line that is not indented (or the end of the article).</p>
<p>Within a code block, <strong><em>ampersands (&amp;)</em></strong> and angle <strong>brackets (&lt; and &gt;)</strong> are automatically converted into HTML entities.</p>
<p>代码：</p>
<pre><code>    &lt;div class="footer"&gt;
        &amp;copy; 2004 Foo Corporation
    &lt;/div&gt;
</code></pre>
<p>预览：</p>
<hr />
<pre><code>&lt;div class="footer"&gt;
    &amp;copy; 2004 Foo Corporation
&lt;/div&gt;
</code></pre>
<hr />
<p>以下部分围栏代码块和语法高亮是扩展功能，你可以使用其他方式编写代码块。</p>
<h4>围栏代码块</h4>
<p>只需将代码包裹在 <code>```</code> 中（如下所示），你就不需要缩进四个空格。</p>
<p>代码：</p>
<pre><code>Here's an example:

```
function test() {
  console.log("notice the blank line before this function?");
}
```
</code></pre>
<p>预览：</p>
<hr />
<p>Here's an example:</p>
<pre><code>function test() {
  console.log("notice the blank line before this function?");
}
</code></pre>
<hr />
<h4>语法高亮</h4>
<p>在你的围栏代码块中，添加可选的语言标识符，我们将通过语法高亮运行它（<a href="https://github.com/github/linguist/blob/master/lib/linguist/languages.yml">支持的语言</a>）。</p>
<p>代码：</p>
<pre><code>```ruby
require 'redcarpet'
markdown = Redcarpet.new("Hello World!")
puts markdown.to_html
```
</code></pre>
<p>预览：</p>
<hr />
<pre><code>require 'redcarpet'
markdown = Redcarpet.new("Hello World!")
puts markdown.to_html
</code></pre>
<hr />
<h3>水平线</h3>
<p>HTML 标签：<code>&lt;hr /&gt;</code>
在一行中单独放置<strong>三个或更多连字符 (-)、星号 (*) 或下划线 (_)</strong>。你可以在连字符或星号之间使用空格。</p>
<p>代码：</p>
<pre><code>* * *
***
*****
- - -
---------------------------------------
___
</code></pre>
<p>预览：</p>
<hr />
<hr />
<hr />
<hr />
<hr />
<hr />
<hr />
<hr />
<h3>表格</h3>
<p>HTML 标签：<code>&lt;table&gt;</code></p>
<p>这是一个扩展功能。</p>
<p>用<strong>管道符 (|)</strong> 分隔列，用<strong>破折号 (-)</strong> 分隔表头，使用<strong>冒号 (:)</strong> 进行对齐。</p>
<p>外层的<strong>管道符 (|)</strong> 和对齐是可选的。每个单元格至少有 <strong>3 个分隔符</strong>用于分隔表头。</p>
<p>代码：</p>
<pre><code>| Left | Center | Right |
|:-----|:------:|------:|
|aaa   |bbb     |ccc    |
|ddd   |eee     |fff    |

 A | B
---|---
123|456


A |B
--|--
12|45
</code></pre>
<p>预览：</p>
<hr />
<table>
<thead>
<tr>
<th>Left</th>
<th>Center</th>
<th>Right</th>
</tr>
</thead>
<tbody>
<tr>
<td>aaa</td>
<td>bbb</td>
<td>ccc</td>
</tr>
<tr>
<td>ddd</td>
<td>eee</td>
<td>fff</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>A</th>
<th>B</th>
</tr>
</thead>
<tbody>
<tr>
<td>123</td>
<td>456</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>A</th>
<th>B</th>
</tr>
</thead>
<tbody>
<tr>
<td>12</td>
<td>45</td>
</tr>
</tbody>
</table>
<hr />
<h2>行内元素</h2>
<h3>链接</h3>
<p>HTML 标签：<code>&lt;a&gt;</code></p>
<p>Markdown 支持两种链接样式：行内和引用。</p>
<h4>行内链接</h4>
<p>行内链接格式如下：<code>[链接文本](URL "标题")</code></p>
<p>标题是可选的。</p>
<p>代码：</p>
<pre><code>This is [an example](http://example.com/ "Title") inline link.

[This link](http://example.net/) has no title attribute.
</code></pre>
<p>预览：</p>
<hr />
<p>This is <a href="http://example.com/">an example</a> inline link.</p>
<p><a href="http://example.net/">This link</a> has no title attribute.</p>
<hr />
<p>如果你引用同一服务器上的本地资源，可以使用相对路径：</p>
<p>代码：</p>
<pre><code>See my [About](/about/) page for details.
</code></pre>
<p>预览：</p>
<hr />
<p>See my <a href="/about/">About</a> page for details.</p>
<hr />
<h4>引用链接</h4>
<p>你可以预定义链接引用。格式如下：<code>[id]: URL "标题"</code></p>
<p>标题也是可选的。然后你引用链接时，格式如下：<code>[链接文本][id]</code></p>
<p>代码：</p>
<pre><code>[id]: http://example.com/  "Optional Title Here"
This is [an example][id] reference-style link.
</code></pre>
<p>预览：</p>
<hr />
<p>This is <a href="http://example.com/">an example</a> reference-style link.</p>
<hr />
<p>That is:</p>
<ul>
<li>Square brackets containing the link identifier (<strong>not case sensitive</strong>, optionally indented from the left margin using up to three spaces);</li>
<li>followed by a colon;</li>
<li>followed by one or more spaces (or tabs);</li>
<li>followed by the URL for the link;</li>
<li>The link URL may, optionally, be surrounded by angle brackets.</li>
<li>optionally followed by a title attribute for the link, enclosed in double or single quotes, or enclosed in parentheses.</li>
</ul>
<p>The following three link definitions are equivalent:</p>
<p>代码：</p>
<pre><code>[foo]: http://example.com/  "Optional Title Here"
[foo]: http://example.com/  'Optional Title Here'
[foo]: http://example.com/  (Optional Title Here)
[foo]: &lt;http://example.com/&gt;  "Optional Title Here"
</code></pre>
<p>Uses an empty set of square brackets, the link text itself is used as the name.</p>
<p>代码：</p>
<pre><code>[Google]: http://google.com/
[Google][]
</code></pre>
<p>预览：</p>
<hr />
<p><a href="http://google.com/">Google</a></p>
<hr />
<h3>强调</h3>
<p>HTML 标签：<code>&lt;em&gt;</code>、<code>&lt;strong&gt;</code></p>
<p>Markdown 将<strong>星号 (*)</strong> 和<strong>下划线 (_)</strong> 视为强调指示符。<strong>一个分隔符</strong>将是 <code>&lt;em&gt;</code>；*<em>双分隔符</em> 将是 <code>&lt;strong&gt;</code>。</p>
<p>代码：</p>
<pre><code>*single asterisks*

_single underscores_

**double asterisks**

__double underscores__
</code></pre>
<p>预览：</p>
<hr />
<p><em>single asterisks</em></p>
<p><em>single underscores</em></p>
<p><strong>double asterisks</strong></p>
<p><strong>double underscores</strong></p>
<hr />
<p>But if you surround an * or _ with spaces, it'll be treated as a literal asterisk or underscore.</p>
<p>You can backslash escape it:</p>
<p>代码：</p>
<pre><code>\*this text is surrounded by literal asterisks\*
</code></pre>
<p>预览：</p>
<hr />
<p>*this text is surrounded by literal asterisks*</p>
<hr />
<h3>代码</h3>
<p>HTML 标签：<code>&lt;code&gt;</code></p>
<p>用<strong>反引号 (`)</strong> 包裹它。</p>
<p>代码：</p>
<pre><code>Use the `printf()` function.
</code></pre>
<p>预览：</p>
<hr />
<p>Use the <code>printf()</code> function.</p>
<hr />
<p>To include a literal backtick character within a code span, you can use <strong>multiple backticks</strong> as the opening and closing delimiters:</p>
<p>代码：</p>
<pre><code>``There is a literal backtick (`) here.``
</code></pre>
<p>预览：</p>
<hr />
<p><code>There is a literal backtick (`) here.</code></p>
<hr />
<p>The backtick delimiters surrounding a code span may include spaces — one after the opening, one before the closing. This allows you to place literal backtick characters at the beginning or end of a code span:</p>
<p>代码：</p>
<pre><code>A single backtick in a code span: `` ` ``

A backtick-delimited string in a code span: `` `foo` ``
</code></pre>
<p>预览：</p>
<hr />
<p>A single backtick in a code span: <code>`</code></p>
<p>A backtick-delimited string in a code span: <code>`foo`</code></p>
<hr />
<h3>图片</h3>
<p>HTML 标签：<code>&lt;img /&gt;</code></p>
<p>Markdown 使用一种图片语法，旨在类似于链接的语法，允许两种样式：行内和引用。</p>
<h4>行内图片</h4>
<p>行内图片语法如下：<code>![替代文本](URL "标题")</code></p>
<p>标题是可选的。</p>
<p>代码：</p>
<pre><code>![Alt text](/path/to/img.jpg)

![Alt text](/path/to/img.jpg "Optional title")
</code></pre>
<p>预览：</p>
<hr />
<p><img src="https://s2.loli.net/2024/08/20/5fszgXeOxmL3Wdv.webp" alt="Alt text" /></p>
<p><img src="https://s2.loli.net/2024/08/20/5fszgXeOxmL3Wdv.webp" alt="Alt text" title="Optional title" /></p>
<hr />
<p>即：</p>
<ul>
<li>感叹号：!；</li>
<li>后跟一组方括号，包含图片的 alt 属性文本；</li>
<li>后跟一组圆括号，包含图片的 URL 或路径，以及用双引号或单引号括起来的可选标题属性。</li>
</ul>
<h4>引用图片</h4>
<p>引用样式的图片语法如下：<code>![替代文本][id]</code></p>
<p>代码：</p>
<pre><code>[img id]: https://s2.loli.net/2024/08/20/5fszgXeOxmL3Wdv.webp  "Optional title attribute"
![Alt text][img id]
</code></pre>
<p>预览：</p>
<hr />
<p><img src="https://s2.loli.net/2024/08/20/5fszgXeOxmL3Wdv.webp" alt="Alt text" title="Optional title attribute" /></p>
<hr />
<h3>删除线</h3>
<p>HTML 标签：<code>&lt;del&gt;</code></p>
<p>这是一个扩展功能。</p>
<p>GFM 添加了删除线文本的语法。</p>
<p>代码：</p>
<pre><code>~~Mistaken text.~~
</code></pre>
<p>预览：</p>
<hr />
<p><s>Mistaken text.</s></p>
<hr />
<h2>其他</h2>
<h3>自动链接</h3>
<p>Markdown 支持一种快捷方式样式来创建 URL 和电子邮件地址的"自动"链接：只需用尖括号包围 URL 或电子邮件地址。</p>
<p>代码：</p>
<pre><code>&lt;http://example.com/&gt;

&lt;address@example.com&gt;
</code></pre>
<p>预览：</p>
<hr />
<p><a href="http://example.com/">http://example.com/</a></p>
<p><a href="mailto:address@example.com">address@example.com</a></p>
<hr />
<p>GFM 会自动链接标准 URL。</p>
<p>代码：</p>
<pre><code>https://github.com/emn178/markdown
</code></pre>
<p>预览：</p>
<hr />
<p>https://github.com/emn178/markdown</p>
<hr />
<h3>反斜杠转义</h3>
<p>Markdown 允许你使用反斜杠转义来生成字面字符，否则这些字符在 Markdown 的格式化语法中具有特殊含义。</p>
<p>代码：</p>
<pre><code>\*literal asterisks\*
</code></pre>
<p>预览：</p>
<hr />
<p>*literal asterisks*</p>
<hr />
<p>Markdown 为以下字符提供反斜杠转义：</p>
<p>代码：</p>
<pre><code>\   backslash
`   backtick
*   asterisk
_   underscore
{}  curly braces
[]  square brackets
()  parentheses
#   hash mark
+   plus sign
-   minus sign (hyphen)
.   dot
!   exclamation mark
</code></pre>
<h2>行内 HTML</h2>
<p>对于 Markdown 语法未涵盖的任何标记，你只需使用 HTML 本身。无需前言或分隔符来指示你从 Markdown 切换到 HTML；你只需使用标签。</p>
<p>代码：</p>
<pre><code>This is a regular paragraph.

&lt;table&gt;
    &lt;tr&gt;
        &lt;td&gt;Foo&lt;/td&gt;
    &lt;/tr&gt;
&lt;/table&gt;

This is another regular paragraph.
</code></pre>
<p>预览：</p>
<hr />
<p>This is a regular paragraph.</p>
<p>&lt;table&gt;
&lt;tr&gt;
&lt;td&gt;Foo&lt;/td&gt;
&lt;/tr&gt;
&lt;/table&gt;</p>
<p>This is another regular paragraph.</p>
<hr />
<p>请注意，Markdown 格式化语法<strong>不会在块级 HTML 标签内处理</strong>。</p>
<p>与块级 HTML 标签不同，Markdown 语法<strong>会在行内级标签内处理</strong>。</p>
<p>代码：</p>
<pre><code>&lt;span&gt;**Work**&lt;/span&gt;

&lt;div&gt;
    **No Work**
&lt;/div&gt;
</code></pre>
<p>预览：</p>
<hr />
<p>&lt;span&gt;<strong>Work</strong>&lt;/span&gt;</p>
<p>&lt;div&gt;
<strong>No Work</strong>
&lt;/div&gt;</p>
<hr />
]]></content>
    <author>
      <name>微雨问海棠</name>
    </author>
    <category term="示例"></category>
  </entry>
  <entry>
    <title>Markdown 扩展功能</title>
    <link href="https://mizuki.mysqil.com/posts/markdown-extended/" rel="alternate" type="text/html"/>
    <id>https://mizuki.mysqil.com/posts/markdown-extended/</id>
    <published>2024-05-01T00:00:00.000Z</published>
    <updated>2024-11-29T00:00:00.000Z</updated>
    <summary>了解更多关于 Mizuki 中的 Markdown 功能</summary>
    <content type="html"><![CDATA[<h2>GitHub 仓库卡片</h2>
<p>你可以添加动态卡片链接到 GitHub 仓库，页面加载时，仓库信息会从 GitHub API 获取。</p>
<p>::github{repo="captain-ht/Mizuki"}</p>
<p>使用代码 <code>::github{repo="captain-ht/Mizuki"}</code> 创建 GitHub 仓库卡片。</p>
<pre><code>::github{repo="captain-ht/Mizuki"}
</code></pre>
<h2>提示框</h2>
<p>支持以下类型的提示框：<code>note</code> <code>tip</code> <code>important</code> <code>warning</code> <code>caution</code></p>
<p>:::note
突出显示用户应该考虑的信息，即使在快速浏览时也是如此。
:::</p>
<p>:::tip
可选的帮助用户更成功的信息。
:::</p>
<p>:::important
用户成功所需的关键信息。
:::</p>
<p>:::warning
由于潜在风险，需要用户立即关注的关键内容。
:::</p>
<p>:::caution
操作可能带来的负面后果。
:::</p>
<h3>基本语法</h3>
<pre><code>:::note
突出显示用户应该考虑的信息，即使在快速浏览时也是如此。
:::

:::tip
可选的帮助用户更成功的信息。
:::
</code></pre>
<h3>自定义标题</h3>
<p>提示框的标题可以自定义。</p>
<p>:::note[我的自定义标题]
这是一个带有自定义标题的提示。
:::</p>
<pre><code>:::note[我的自定义标题]
这是一个带有自定义标题的提示。
:::
</code></pre>
<h3>GitHub 语法</h3>
<blockquote>
<p>[!TIP]
也支持 <a href="https://github.com/orgs/community/discussions/16925">GitHub 语法</a>。</p>
</blockquote>
<pre><code>&gt; [!NOTE]
&gt; 也支持 GitHub 语法。

&gt; [!TIP]
&gt; 也支持 GitHub 语法。
</code></pre>
<h3>剧透</h3>
<p>你可以在文本中添加剧透。文本也支持 <strong>Markdown</strong> 语法。</p>
<p>内容 :spoiler[是隐藏的 <strong>ayyy</strong>]！</p>
<pre><code>内容 :spoiler[是隐藏的 **ayyy**]！</code></pre>
]]></content>
    <author>
      <name>微雨问海棠</name>
    </author>
    <category term="示例"></category>
  </entry>
  <entry>
    <title>Mizuki 简单指南</title>
    <link href="https://mizuki.mysqil.com/posts/guide/" rel="alternate" type="text/html"/>
    <id>https://mizuki.mysqil.com/posts/guide/</id>
    <published>2024-04-01T00:00:00.000Z</published>
    <updated>2024-04-01T00:00:00.000Z</updated>
    <summary>如何使用这个博客模板。</summary>
    <content type="html"><![CDATA[<p>这个博客模板使用 <a href="https://astro.build/">Astro</a> 构建。对于本指南中未提及的内容，你可以在 <a href="https://docs.astro.build/">Astro 文档</a> 中找到答案。</p>
<h2>文章的前置元数据</h2>
<pre><code>---
title: 我的第一篇博客文章
published: 2023-09-09
description: 这是我新 Astro 博客的第一篇文章。
image: ./cover.jpg
tags: [Foo, Bar]
category: 前端
draft: false
---
</code></pre>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>title</code></td>
<td>文章的标题。</td>
</tr>
<tr>
<td><code>published</code></td>
<td>文章发布的日期。</td>
</tr>
<tr>
<td><code>pinned</code></td>
<td>是否将此文章置顶到文章列表顶部。</td>
</tr>
<tr>
<td><code>description</code></td>
<td>文章的简短描述。显示在首页。</td>
</tr>
<tr>
<td><code>image</code></td>
<td>文章的封面图片路径。&lt;br/&gt;1. 以 <code>http://</code> 或 <code>https://</code> 开头：使用网络图片&lt;br/&gt;2. 以 <code>/</code> 开头：使用 <code>public</code> 目录中的图片&lt;br/&gt;3. 无前缀：相对于 markdown 文件</td>
</tr>
<tr>
<td><code>tags</code></td>
<td>文章的标签。</td>
</tr>
<tr>
<td><code>category</code></td>
<td>文章的分类。</td>
</tr>
<tr>
<td><code>licenseName</code></td>
<td>文章内容的许可证名称。</td>
</tr>
<tr>
<td><code>author</code></td>
<td>文章的作者。</td>
</tr>
<tr>
<td><code>sourceLink</code></td>
<td>文章内容的来源链接或参考。</td>
</tr>
<tr>
<td><code>draft</code></td>
<td>如果文章仍是草稿，将不会显示。</td>
</tr>
</tbody>
</table>
<h2>文章文件的存放位置</h2>
<p>你的文章文件应该放在 <code>src/content/posts/</code> 目录中。你也可以创建子目录来更好地组织你的文章和资源。</p>
<pre><code>src/content/posts/
├── post-1.md
└── post-2/
    ├── cover.png
    └── index.md
</code></pre>
]]></content>
    <author>
      <name>微雨问海棠</name>
    </author>
    <category term="指南"></category>
  </entry>
  <entry>
    <title>Markdown Mermaid</title>
    <link href="https://mizuki.mysqil.com/posts/markdown-mermaid/" rel="alternate" type="text/html"/>
    <id>https://mizuki.mysqil.com/posts/markdown-mermaid/</id>
    <published>2023-10-01T00:00:00.000Z</published>
    <updated>2023-10-01T00:00:00.000Z</updated>
    <summary>一个带有 Mermaid 图表的 Markdown 博客文章示例。</summary>
    <content type="html"><![CDATA[<h1>Markdown 中使用 Mermaid 图表的完整指南</h1>
<p>本文演示了如何在 Markdown 文档中使用 Mermaid 创建各种复杂的图表，包括流程图、时序图、甘特图、类图和状态图。</p>
<h2>流程图示例</h2>
<p>流程图非常适合表示流程或算法步骤。</p>
<pre><code>graph TD
    A[Start] --&gt; B{Condition Check}
    B --&gt;|Yes| C[Process Step 1]
    B --&gt;|No| D[Process Step 2]
    C --&gt; E[Subprocess]
    D --&gt; E
    subgraph E [Subprocess Details]
        E1[Substep 1] --&gt; E2[Substep 2]
        E2 --&gt; E3[Substep 3]
    end
    E --&gt; F{Another Decision}
    F --&gt;|Option 1| G[Result 1]
    F --&gt;|Option 2| H[Result 2]
    F --&gt;|Option 3| I[Result 3]
    G --&gt; J[End]
    H --&gt; J
    I --&gt; J
</code></pre>
<h2>时序图示例</h2>
<p>时序图显示对象随时间变化的交互。</p>
<pre><code>sequenceDiagram
    participant User
    participant WebApp
    participant Server
    participant Database

    User-&gt;&gt;WebApp: Submit Login Request
    WebApp-&gt;&gt;Server: Send Auth Request
    Server-&gt;&gt;Database: Query User Credentials
    Database--&gt;&gt;Server: Return User Data
    Server--&gt;&gt;WebApp: Return Auth Result
    
    alt Auth Successful
        WebApp-&gt;&gt;User: Show Welcome Page
        WebApp-&gt;&gt;Server: Request User Data
        Server-&gt;&gt;Database: Get User Preferences
        Database--&gt;&gt;Server: Return Preferences
        Server--&gt;&gt;WebApp: Return User Data
        WebApp-&gt;&gt;User: Load Personalized Interface
    else Auth Failed
        WebApp-&gt;&gt;User: Show Error Message
        WebApp-&gt;&gt;User: Prompt Re-entry
    end
</code></pre>
<h2>甘特图示例</h2>
<p>甘特图非常适合显示项目进度和时间线。</p>
<pre><code>gantt
    title Website Development Project Timeline
    dateFormat  YYYY-MM-DD
    axisFormat  %m/%d
    
    section Design Phase
    Requirements Analysis      :a1, 2023-10-01, 7d
    UI Design                 :a2, after a1, 10d
    Prototype Creation        :a3, after a2, 5d
    
    section Development Phase
    Frontend Development      :b1, 2023-10-20, 15d
    Backend Development       :b2, after a2, 18d
    Database Design           :b3, after a1, 12d
    
    section Testing Phase
    Unit Testing              :c1, after b1, 8d
    Integration Testing       :c2, after b2, 10d
    User Acceptance Testing   :c3, after c2, 7d
    
    section Deployment
    Production Deployment     :d1, after c3, 3d
    Launch                    :milestone, after d1, 0d
</code></pre>
<h2>类图示例</h2>
<p>类图显示系统的静态结构，包括类、属性、方法及其关系。</p>
<pre><code>classDiagram
    class User {
        +String username
        +String password
        +String email
        +Boolean active
        +login()
        +logout()
        +updateProfile()
    }
    
    class Article {
        +String title
        +String content
        +Date publishDate
        +Boolean published
        +publish()
        +edit()
        +delete()
    }
    
    class Comment {
        +String content
        +Date commentDate
        +addComment()
        +deleteComment()
    }
    
    class Category {
        +String name
        +String description
        +addArticle()
        +removeArticle()
    }
    
    User "1" -- "*" Article : writes
    User "1" -- "*" Comment : posts
    Article "1" -- "*" Comment : has
    Article "1" -- "*" Category : belongs to
</code></pre>
<h2>状态图示例</h2>
<p>状态图显示对象在其生命周期中经历的状态序列。</p>
<pre><code>stateDiagram-v2
    [*] --&gt; Draft
    
    Draft --&gt; UnderReview : submit
    UnderReview --&gt; Draft : reject
    UnderReview --&gt; Approved : approve
    Approved --&gt; Published : publish
    Published --&gt; Archived : archive
    Published --&gt; Draft : retract
    
    state Published {
        [*] --&gt; Active
        Active --&gt; Hidden : temporarily hide
        Hidden --&gt; Active : restore
        Active --&gt; [*]
        Hidden --&gt; [*]
    }
    
    Archived --&gt; [*]
</code></pre>
<h2>饼图示例</h2>
<p>饼图非常适合显示比例和百分比数据。</p>
<pre><code>pie title Website Traffic Sources Analysis
    "Search Engines" : 45.6
    "Direct Access" : 30.1
    "Social Media" : 15.3
    "Referral Links" : 6.4
    "Other Sources" : 2.6
</code></pre>
<h2>总结</h2>
<p>Mermaid 是在 Markdown 文档中创建各种类型图表的强大工具。本文演示了如何使用流程图、时序图、甘特图、类图、状态图和饼图。这些图表可以帮助你更清晰地表达复杂的概念、流程和数据结构。</p>
<p>要使用 Mermaid，只需在代码块中指定 mermaid 语言，并使用简洁的文本语法描述图表。Mermaid 会自动将这些描述转换为美观的可视化图表。</p>
<p>在你的下一篇技术博客文章或项目文档中尝试使用 Mermaid 图表——它们会让你的内容更加专业和易于理解！</p>
]]></content>
    <author>
      <name>微雨问海棠</name>
    </author>
    <category term="示例"></category>
  </entry>
  <entry>
    <title>在文章中嵌入视频</title>
    <link href="https://mizuki.mysqil.com/posts/video/" rel="alternate" type="text/html"/>
    <id>https://mizuki.mysqil.com/posts/video/</id>
    <published>2022-08-01T00:00:00.000Z</published>
    <updated>2022-08-01T00:00:00.000Z</updated>
    <summary>这篇文章演示了如何在博客文章中嵌入视频。</summary>
    <content type="html"><![CDATA[<p>只需从 YouTube 或其他平台复制嵌入代码，然后粘贴到 markdown 文件中。</p>
<pre><code>---
title: 在文章中嵌入视频
published: 2023-10-19
// ...
---

&lt;iframe width="100%" height="468" src="https://www.youtube.com/embed/5gIf0_xpFPI?si=N1WTorLKL0uwLsU_" title="YouTube video player" frameborder="0" allowfullscreen&gt;&lt;/iframe&gt;
</code></pre>
<h2>YouTube</h2>
<p>&lt;iframe width="100%" height="468" src="https://www.youtube.com/embed/5gIf0_xpFPI?si=N1WTorLKL0uwLsU_" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen&gt;&lt;/iframe&gt;</p>
<h2>Bilibili</h2>
<p>&lt;iframe width="100%" height="468" src="//player.bilibili.com/player.html?bvid=BV1fK4y1s7Qf&amp;p=1&amp;autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true" &amp;autoplay=0&gt; &lt;/iframe&gt;</p>
]]></content>
    <author>
      <name>微雨问海棠</name>
    </author>
    <category term="示例"></category>
  </entry>
</feed>